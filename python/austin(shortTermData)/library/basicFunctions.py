#This is basic functions that aren't that important to have a script for but important enough to remember how to use it. Basically useful junk...

import webbrowser

def open_browser(link):
    webbrowser.open_new_tab(link)